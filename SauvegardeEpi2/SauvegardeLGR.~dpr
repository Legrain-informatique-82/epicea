program SauvegardeLGR;

uses
  Forms,
  S2_Sauv in 'S2_Sauv.pas' {Sauv},
  S2_MsgForm in 'S2_MsgForm.pas' {MsgForm},
  UMail in '..\..\LibrairieDev\Delphi\LibEpi\UMail.pas',
  libLocale in 'libLocale.pas',
  MyBackup in 'MyBackup.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TSauv, Sauv);
  Application.CreateForm(TMsgForm, MsgForm);
  Application.Run;
end.
